apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: spread-service-ingress
  namespace: spread-service
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: web
spec:
  rules:
    - host: spread.46.255.6.40.nip.io
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: api-server
                port:
                  number: 3000
